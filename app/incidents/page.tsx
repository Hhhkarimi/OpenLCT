"use client";
import { useScenario } from "../../components/ScenarioProvider";
import { RiskBadge,SectionTitle } from "../../components/ui";
export default function Page(){const {snapshot}=useScenario();return <><section className="hero compact"><div><div className="eyebrow">INCIDENT CENTER</div><h1>مرکز رخداد و اثر زنجیره‌ای</h1><p>Root cause، دامنه اثر و اقدام پیشنهادی را برای هر اختلال یک‌جا مشاهده کن.</p></div></section><SectionTitle title="رخدادهای فعال" subtitle={snapshot.incidents.length+" رخداد در سناریوی فعلی"}/><section className="incident-grid">{snapshot.incidents.map(i=><article className="card incident-card" key={i.id}><div className="incident-card-head"><div><span className="mono">{i.id}</span><h2>{i.title}</h2></div><RiskBadge level={i.severity}/></div><dl className="incident-details"><div><dt>لایه</dt><dd>{i.layer}</dd></div><div><dt>Root cause</dt><dd>{i.rootCause}</dd></div><div><dt>دامنه اثر</dt><dd>{i.affected}</dd></div><div><dt>اقدام</dt><dd>{i.action}</dd></div><div><dt>اثر مورد انتظار</dt><dd>{i.expectedImpact}</dd></div></dl></article>)}</section></>;}
